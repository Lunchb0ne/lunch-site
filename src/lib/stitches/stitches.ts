// https://stitches.dev/docs/utils
// Using stitches bundled in nextui
import { CSS as NextCSS, styled } from '@nextui-org/react';

export type CSS = NextCSS;

export const StyledComponent = styled('div');
